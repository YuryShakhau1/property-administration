spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/property_administration
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:password}

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect